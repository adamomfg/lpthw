#!/usr/bin/python
from sys import argv
from os.path import exists

scropt, from_file, to_file = argv

# we coudl do these two on one lin too, how?
input = open(from_file)
indata = input.read()

print )
